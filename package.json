{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Smilin Jasper",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.ts",
    "dev": "node --watch index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../notes-frontend && npm run build && cp -r ./dist ../Backend",
    "deploy:full": "npm run build:ui && git add ./ && git commit -m build-and-deploy-app"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/morgan": "^1.9.10",
    "@types/node": "^25.6.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.6.2",
    "morgan": "^1.10.1"
  }
}
